apiVersion: v1
kind: Secret
metadata:
  name: multiagent-secrets
  namespace: multiagent
type: Opaque
stringData:
  deepseek-api-key: "REPLACE_WITH_ACTUAL_KEY"
  langsmith-api-key: "REPLACE_WITH_ACTUAL_KEY"
  postgres-user: "multiagent"
  postgres-password: "REPLACE_WITH_PASSWORD"
